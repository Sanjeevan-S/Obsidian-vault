In the context of **Server-Sent Events (SSE)**, an "event" is the fundamental unit of data sent from the server to the client. Think of it as a small, discrete packet of information that travels through a persistent "pipe."

The **EventSource** is the Web API on the client-side (the browser) used to open that pipe and listen for these incoming events.
[[EventSource Interface]]


